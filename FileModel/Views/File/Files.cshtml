



@{
    @model IEnumerable<FileModel.Models.FileClass>
}


@{
    ViewBag.Title = "Files";
}
<head>
    <style type="text/css">
        .auto-style {
            color: #FFFFFF;
        }

        .t {
            text-align: center;
            vertical-align: middle;
        }
    </style>
</head>
<h2>Table with document types</h2>
<h3> Please see below:</h3>


@{
    <div style="height: 400px; overflow: auto;">
        <div class="well-sm">
            <table class="table table-condensed table-hover">
                <thead style="position: sticky;top: 0">
                    <tr bgcolor="#222222" class="auto-style">
                        <th class="col-lg-8">File Name</th>
                        <th class="t, col-lg-2">DOWNLOAD</th>
                        <th class="t, col-lg-2">DELETE</th>
                    </tr>
                </thead>
                @foreach (FileModel.Models.FileClass file in Model)
                {
                    <tr>
                        @*
                            Default >> @Html.DisplayFor(modelItem => file.FileName)
                        *@

                        <td>@file.FileName</td>
                        <td>
                            <button>
                                @Html.ActionLink("Download", "DownloadFile",
                                 new { fileName = file.FileName })
                            </button>
                        </td>
                        <td>
                            <button>
                                @Html.ActionLink("DELETE", "DeleteFile",
                               new { fileName = file.FileName },
                               new { onclick = "return confirm('Are you sure you wish to delete this?');" })
                            </button>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
}